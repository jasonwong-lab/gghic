% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/tidy-methods.R
\name{tidy}
\alias{tidy}
\alias{tidy,MultiWayContacts-method}
\title{Tidy hypergraph to long format}
\usage{
tidy(x, ...)

\S4method{tidy}{MultiWayContacts}(x, max_hyperedges = NULL, weight_normalization = "none")
}
\arguments{
\item{x}{MultiWayContacts with built hypergraph.}

\item{...}{Additional arguments (not used).}

\item{max_hyperedges}{Integer or NULL. Maximum hyperedges to include
(default: NULL for all). Selects top by contact order.}

\item{weight_normalization}{Character. Weight scheme: \code{"none"} (raw),
\code{"log"}, \code{"by_order"}, or \code{"minmax"} (default: \code{"none"}).}
}
\value{
MultiWayContacts with \code{tidied_hypergraph} slot containing: bin_idx,
hyperedge_idx, bin_id, chrom, bin, n_multiways, weight.
}
\description{
Converts hypergraph incidence matrix to long-format data frame for analysis
and visualization.
}
\details{
Converts sparse matrix to long format. When \code{max_hyperedges} is set, ranks
by contact order and retains top N. Unused bins are removed.
}
\examples{
\dontrun{
# Tidy all with raw weights
mc <- MultiWayContacts("sample.pairs.gz") |> import() |> build() |> tidy()

# Top 100 with log weights
mc <- mc |> tidy(max_hyperedges = 100, weight_normalization = "log")
}

}
