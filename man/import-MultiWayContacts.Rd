% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-methods.R
\name{import-MultiWayContacts}
\alias{import-MultiWayContacts}
\alias{import,MultiWayContacts,ANY,ANY-method}
\title{Import multi-way contact data}
\usage{
\S4method{import}{MultiWayContacts,ANY,ANY}(con, format, text, inter_chrom = FALSE, ...)
}
\arguments{
\item{con}{A \code{MultiWayContacts} object containing the path to the pairs file
and optional focus regions.}

\item{format}{Logical indicating whether to include inter-chromosomal
contacts. This parameter is used as the second positional argument for
convenience. Default is \code{FALSE}.}

\item{text}{Not used. Included for compatibility with the generic.}

\item{inter_chrom}{Logical. If \code{TRUE}, includes inter-chromosomal contacts.
If \code{FALSE} (default), only intra-chromosomal contacts are loaded. If
\code{format} is provided, it overrides this parameter.}

\item{...}{Additional arguments (currently not used).}
}
\value{
A \code{MultiWayContacts} object with the \code{pairs} slot populated by a
data frame containing:
\itemize{
\item read_name: Unique identifier for each read
\item chrom1, chrom2: Chromosomes for each contact
\item pos1, pos2: Genomic positions for each contact
}
}
\description{
Imports multi-way chromatin contact data from a .pairs file into a
MultiWayContacts object. The method loads pairwise contacts from long-read
sequencing experiments (e.g., Pore-C).
}
\details{
The function loads pairwise contact data from a .pairs file format (typically
.pairs.gz). If a focus chromosome is specified in the \code{MultiWayContacts}
object, only contacts involving that chromosome are loaded.

When \code{inter_chrom = FALSE}, reads that contain any inter-chromosomal
contacts are completely removed from the dataset to ensure clean
intra-chromosomal analysis.

The pairs file is read using optimized C code for efficient parsing of
large files.

For convenience, the inter_chrom parameter can be provided as the second
positional argument: \code{import(mc, TRUE)} is equivalent to
\code{import(mc, inter_chrom = TRUE)}.
}
\examples{
\dontrun{
# Basic import (intra-chromosomal only)
mc <- MultiWayContacts("sample.pairs.gz")
mc <- import(mc)

# Using pipe operator
mc <- MultiWayContacts("sample.pairs.gz") |> import()

# Include inter-chromosomal contacts
mc <- import(mc, inter_chrom = TRUE)

# Convenient syntax
mc <- import(mc, TRUE)

# Import with focus on specific chromosome
mc <- MultiWayContacts(
  "sample.pairs.gz",
  focus = "chr1"
) |> import()

# Check imported data
head(mc@pairs)
}

}
