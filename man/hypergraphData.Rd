% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/getters-methods.R
\name{hypergraphData}
\alias{hypergraphData}
\alias{hypergraphData,MultiWayContacts-method}
\title{Get hypergraph data from MultiWayContacts object}
\usage{
hypergraphData(x, which = c("selected", "tidied"))

\S4method{hypergraphData}{MultiWayContacts}(x, which = c("selected", "tidied"))
}
\arguments{
\item{x}{A \code{MultiWayContacts} object.}

\item{which}{Character. Which hypergraph data to retrieve: \code{"selected"}
(default) returns the selected subset for visualization, \code{"tidied"}
returns the full tidied long-format data.}
}
\value{
A data.frame containing hypergraph data in long format, or NULL if
the requested data has not been computed yet.
}
\description{
Retrieves the selected or tidied hypergraph data from a \code{MultiWayContacts}
object.
}
\details{
\itemize{
\item \code{which = "selected"}: Returns data from \code{select()}, a subset of hyperedges
chosen for visualization. Use this for plotting.
\item \code{which = "tidied"}: Returns data from \code{tidy()}, the full hypergraph in
long format. Use this for analysis.
}
}
\examples{
\dontrun{
mc <- MultiWayContacts("sample.pairs.gz") |>
  import() |>
  build(bin_size = 1e6, min_multiway = 3) |>
  tidy(max_hyperedges = 100) |>
  select(n = 50)

# Get selected hyperedges for plotting
df <- hypergraphData(mc, "selected")

# Get full tidied data for analysis
df_full <- hypergraphData(mc, "tidied")
}

}
