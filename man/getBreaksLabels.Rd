% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{getBreaksLabels}
\alias{getBreaksLabels}
\alias{get_breaks_labels}
\title{Generate axis breaks and labels for multi-chromosome plots}
\usage{
getBreaksLabels(data)
}
\arguments{
\item{data}{A data frame or tibble with columns \code{seqnames}, \code{start}, and
\code{end} representing genomic coordinates.}
}
\value{
A list with two elements:
\itemize{
\item \code{breaks}: Numeric vector of x-axis break positions
\item \code{labels}: Character vector of formatted labels
}
}
\description{
Calculates appropriate breaks and labels for x-axis when visualizing
multiple chromosomes simultaneously. Adjusts break density based on
chromosome length.
}
\details{
The function:
\enumerate{
\item Groups data by chromosome
\item Calculates chromosome lengths
\item Assigns more breaks to longer chromosomes (2-10 breaks per chromosome)
\item Formats labels in megabases (M)
\item Adds newlines before chromosome transitions for clarity
}

Useful for multi-chromosome Hi-C visualizations where chromosomes are
displayed side-by-side.
}
\examples{
\dontrun{
# Get breaks and labels for multi-chromosome plot
df <- scaleData(cc, "balanced", log10)
breaks_labels <- getBreaksLabels(df)

ggplot(df) +
  geom_hic() +
  scale_x_continuous(
    breaks = breaks_labels$breaks,
    labels = breaks_labels$labels
  )
}

}
