% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-hypergraph.R
\name{geom_hypergraph}
\alias{geom_hypergraph}
\title{Visualize Multi-way Contacts as Hypergraph}
\usage{
geom_hypergraph(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  line_width = 0.3,
  line_alpha = 0.6,
  point_size = 2,
  point_alpha = 0.8,
  colour_by = "n_contacts",
  palette = "viridis",
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{aes()}. Only
\code{x}, \code{y}, and \code{group} are used.}

\item{data}{A data frame from \code{hypergraph_to_tidy()}.}

\item{stat}{The statistical transformation to use on the data.
Default: "identity".}

\item{position}{Position adjustment. Default: "identity".}

\item{na.rm}{If \code{FALSE} (default),
missing values are removed with a warning.}

\item{show.legend}{Logical. Should this layer be included in legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}

\item{line_width}{Numeric line width for hyperedges. Default: 0.3.}

\item{line_alpha}{Numeric transparency for lines. Default: 0.6.}

\item{point_size}{Numeric size of bin points. Default: 2.}

\item{point_alpha}{Numeric transparency for points. Default: 0.8.}

\item{colour_by}{Character: "n_contacts" or "none". Default: "n_contacts".}

\item{palette}{Character: ggplot2 colour palette. Default: "viridis".}

\item{...}{Additional arguments passed to layer.}
}
\description{
Creates a hypergraph visualization for multi-way chromatin contacts where
genomic bins are displayed as points (nodes) and reads are shown as vertical
lines (hyperedges) connecting multiple bins.
}
\details{
This geom creates a specialized visualization for multi-way chromatin
contacts from Pore-C/HiPore-C data. The visualization shows:
\itemize{
\item \strong{Y-axis}: Genomic bins (sorted by position)
\item \strong{X-axis}: Individual reads (ordered by first appearance)
\item \strong{Lines}: Connect all bins contacted by a single read
\item \strong{Points}: Individual bin positions
\item \strong{colour}: Number of contacts per read (optional)
}

The visualization is similar to a Sankey diagram but specialized for
multi-way genomic interactions.
}
\section{Aesthetics}{

\code{geom_hypergraph()} understands the following aesthetics:
- \code{x} (required): Read index or position
- \code{y} (required): Bin position
- \code{group} (required): Read identifier
- \code{colour} (optional): Number of contacts per read
}

\examples{
\dontrun{
# Build hypergraph
hg <- build_hypergraph(
  pairs = pairs_df,
  bin_size = 100000,
  chrom = "chr22",
  quantile = 0.85,
  min_multiway = 3
)

# Convert to tidy format
df <- hypergraph_to_tidy(hg, max_reads = 100)

# Create visualization
ggplot(df, aes(x = read_idx, y = bin, group = read_idx)) +
  geom_hypergraph(aes(colour = n_contacts)) +
  labs(title = "Multi-way Contacts on chr22") +
  theme_minimal()

# Without colouring
ggplot(df, aes(x = read_idx, y = bin, group = read_idx)) +
  geom_hypergraph(colour_by = "none") +
  theme_minimal()
}

}
