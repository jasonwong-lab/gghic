% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/select-methods.R
\name{select}
\alias{select}
\alias{select,MultiWayContacts-method}
\title{Select top hyperedges for visualization}
\usage{
select(x, ...)

\S4method{select}{MultiWayContacts}(
  x,
  n_intra = 5,
  n_inter = 5,
  n_multiways_filter = NULL,
  chroms = NULL,
  append = TRUE,
  ...
)
}
\arguments{
\item{x}{MultiWayContacts with tidied hypergraph.}

\item{...}{Additional arguments (not used).}

\item{n_intra}{Integer. Top intra-chromosomal hyperedges per chromosome
(default: 5).}

\item{n_inter}{Integer. Top inter-chromosomal hyperedges per chromosome
(default: 5). Quota increases if fewer intra hyperedges available.}

\item{n_multiways_filter}{Integer vector or NULL. Filter by specific contact
orders, e.g., \code{c(3,4)} for 3-way and 4-way only (default: NULL).}

\item{chroms}{Character or NULL. Filter by chromosome(s) (default: NULL).}

\item{append}{Logical. Append to existing selection (TRUE) or replace
(FALSE, default: TRUE).}
}
\value{
MultiWayContacts with \code{select_hypergraph} slot containing selected
hyperedges plus \code{type} (intra/inter) and \code{source_chrom} columns.
}
\description{
Selects top-weighted intra- and inter-chromosomal hyperedges per chromosome
for visualization.
}
\details{
Per chromosome: select top \code{n_intra} intra-chromosomal and \code{n_inter}
inter-chromosomal hyperedges by weight. If insufficient intra hyperedges,
quota transferred to inter. Hyperedges deduplicated across chromosomes.
}
\examples{
\dontrun{
# Default: top 5 intra + 5 inter per chromosome
mc <- MultiWayContacts("sample.pairs.gz") |>
  import() |> build() |> tidy() |> select()

# More hyperedges
mc <- mc |> select(n_intra = 10, n_inter = 10, append = FALSE)

# Filter by contact order
mc <- mc |> select(n_multiways_filter = c(3, 4), append = FALSE)

# Specific chromosomes
mc <- mc |> select(chroms = c("chr1", "chr2"), append = FALSE)
}

}
