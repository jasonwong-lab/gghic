% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coerce.R
\name{as_tibble,ChromatinContacts-method}
\alias{as_tibble,ChromatinContacts-method}
\title{Convert ChromatinContacts to tibble}
\usage{
\S4method{as_tibble}{ChromatinContacts}(
  x,
  which = c("interactions", "compartments", "TADs", "loops", "multi_contacts")
)
}
\arguments{
\item{x}{A \code{ChromatinContacts} object.}

\item{which}{Character string. Which data to convert: \code{"interactions"}
(default) for Hi-C interactions, or a feature name like \code{"TADs"},
\code{"loops"}, \code{"compartments"}, or \code{"multi_contacts"}.}
}
\value{
A tibble containing the requested data.
}
\description{
Converts interaction data or feature data from a \code{ChromatinContacts} object
to a tibble (data frame).
}
\examples{
\dontrun{
cc <- ChromatinContacts("sample.cool") |> import()

# Convert interactions to tibble
df <- as_tibble(cc)

# Convert specific feature
tads_df <- as_tibble(cc, which = "TADs")
}

}
