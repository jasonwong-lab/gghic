% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/gghypergraph-methods.R
\name{gghypergraph}
\alias{gghypergraph}
\alias{gghypergraph,MultiWayContacts-method}
\title{Visualize multi-way chromatin contacts as hypergraph network}
\usage{
gghypergraph(x, ...)

\S4method{gghypergraph}{MultiWayContacts}(
  x,
  point_size = 2,
  line_width = 0.3,
  line_alpha = 0.6,
  color_by = "n_multiways",
  palette = "viridis",
  facet_chrom = TRUE,
  weight_normalization = "none"
)
}
\arguments{
\item{x}{MultiWayContacts object with selected hyperedges (after running
\code{\link[=select]{select()}}).}

\item{...}{Additional arguments (not used).}

\item{point_size}{Numeric. Size of genomic bin points (default: 2).}

\item{line_width}{Numeric. Base line width for hyperedges. Actual width
scaled by hyperedge weight (default: 0.3).}

\item{line_alpha}{Numeric between 0 and 1. Line transparency to reduce
overplotting (default: 0.6).}

\item{color_by}{Character. Variable for line coloring:
\itemize{
\item \code{"n_multiways"}: Color by contact order (2-way, 3-way, etc.) (default)
\item Other values: Uniform gray coloring
}}

\item{palette}{Character. Color palette name:
\itemize{
\item Viridis options: \code{"viridis"}, \code{"magma"}, \code{"plasma"}, \code{"inferno"},
\code{"cividis"}
\item ColorBrewer palettes: \code{"YlOrRd"}, \code{"Blues"}, etc.
\item Default: \code{"viridis"}
}}

\item{facet_chrom}{Logical. Layout strategy:
\itemize{
\item \code{TRUE}: Separate facet panel per chromosome (default)
\item \code{FALSE}: Composite y-axis with all chromosomes stacked
}}

\item{weight_normalization}{Character. Deprecated parameter (weights now set
in \code{\link[=tidy]{tidy()}} method).}
}
\value{
A ggplot2 object that can be further customized with additional
ggplot2 layers and themes.
}
\description{
Creates an intuitive ggplot2 visualization of multi-way chromatin contacts
from Pore-C or similar long-read technologies. Displays genomic bins as
points and multi-way contacts as connecting lines (hyperedges), with line
width and color encoding contact properties.
}
\details{
\subsection{Visualization structure}{
\itemize{
\item \strong{X-axis}: Hyperedge index (each read/contact)
\item \strong{Y-axis}: Genomic bin position (genomic coordinate)
\item \strong{Points}: Individual genomic bins
\item \strong{Lines}: Connect all bins in a single multi-way contact
\item \strong{Line width}: Scaled by hyperedge weight
\item \strong{Line color}: Contact order (number of fragments per read)
}
}

\subsection{Interpretation}{
\itemize{
\item Vertical lines: Bins contacted by same read
\item Line thickness: Relative importance/weight
\item Color gradient: Contact complexity (darker = higher-order)
\item Patterns reveal genome organization and contact preferences
}
}

\subsection{Prerequisites}{

Must run complete workflow: \code{import()} → \code{build()} → \code{tidy()} → \code{select()}
before visualization.
}
}
\examples{
\dontrun{
# Complete workflow
mc <- MultiWayContacts("sample.pairs.gz", focus = "chr1") |>
  import() |>
  build(bin_size = 1000000L, quantile = 0.85) |>
  tidy(weight_normalization = "log") |>
  select(n_intra = 10, n_inter = 5)

# Basic visualization
gghypergraph(mc)

# Composite layout with custom colors
gghypergraph(mc, facet_chrom = FALSE, palette = "magma")

# Emphasize line patterns
gghypergraph(mc, line_width = 0.5, line_alpha = 0.8, point_size = 3)

# Further customization
gghypergraph(mc) +
  theme_minimal() +
  labs(title = "Multi-way Contacts on Chromosome 1")
}

}
\seealso{
\code{\link[=MultiWayContacts]{MultiWayContacts()}}, \code{\link[=select]{select()}}, \code{\link[=tidy]{tidy()}}, \code{\link[=build]{build()}}
}
