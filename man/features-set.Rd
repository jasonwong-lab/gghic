% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setters-methods.R
\name{features<-,ChromatinContacts,character,GRanges-method}
\alias{features<-,ChromatinContacts,character,GRanges-method}
\alias{features<-,ChromatinContacts,character,GInteractions-method}
\alias{features<-,ChromatinContacts,character,GRangesList-method}
\alias{features<-,ChromatinContacts,character,list-method}
\title{Set genomic features in ChromatinContacts object}
\usage{
\S4method{features}{ChromatinContacts,character,GRanges}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,GInteractions}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,GRangesList}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,list}(x, name, ...) <- value
}
\arguments{
\item{x}{A \code{ChromatinContacts} object.}

\item{name}{Character string. Must be \code{"tracks"}.}

\item{...}{Additional arguments. Can include \code{which} to specify track names.}

\item{value}{A \code{GRangesList} object containing track data. Each element
should be a \code{GRanges} object representing a single track.}
}
\value{
The modified \code{ChromatinContacts} object.

The modified \code{ChromatinContacts} object.

The modified \code{ChromatinContacts} object.
}
\description{
Assigns genomic features (TADs, compartments, multi_contacts, or tracks) to
a \code{ChromatinContacts} object. The features are automatically confined to
overlap with the imported interaction regions.

Assigns chromatin loop data to a \code{ChromatinContacts} object. Loops are
automatically confined to overlap with the imported interaction regions.

Assigns genomic signal tracks (e.g., ChIP-seq, ATAC-seq) to a
\code{ChromatinContacts} object. Tracks are automatically confined to overlap with
the imported interaction regions.
}
\details{
Features are automatically subsetted to only include regions that overlap
with the imported interaction data. This ensures consistency when subsetting
the \code{ChromatinContacts} object.

\strong{Note:} For tracks, use \code{GRangesList} instead. If a single \code{GRanges} is
provided for tracks, it will be converted to a \code{GRangesList} with one element.

\strong{Important:} Interactions must be imported before features can be assigned.

Loops are stored as \code{GInteractions} objects representing paired genomic
regions. They are automatically subsetted to only include loops that overlap
with the imported interaction data.

\strong{Important:} Interactions must be imported before loops can be assigned.

Tracks should be provided as a \code{GRangesList} where each element represents
a different signal track. If the list is unnamed, default names
(\code{track1}, \code{track2}, etc.) will be assigned.

\strong{Important:} Interactions must be imported before tracks can be assigned.
}
\examples{
\dontrun{
# Import interactions first
cc <- ChromatinContacts("sample.cool") |> import()

# Add TADs
tads <- rtracklayer::import("TADs.bed")
features(cc, "TADs") <- tads

# Add compartments
features(cc, "compartments") <- compartments_gr
}

\dontrun{
# Import interactions first
cc <- ChromatinContacts("sample.cool") |> import()

# Add loops
loops <- rtracklayer::import("loops.bedpe") |>
  makeGInteractionsFromGRangesPairs()
features(cc, "loops") <- loops
}

\dontrun{
# Import interactions first
cc <- ChromatinContacts("sample.cool") |> import()

# Add tracks from BigWig files
track1 <- rtracklayer::import("signal1.bw")
track2 <- rtracklayer::import("signal2.bw")
tracks <- GRangesList(ChIP1 = track1, ChIP2 = track2)
features(cc, "tracks") <- tracks_list
}

}
\seealso{
\code{\link[=features]{features()}}

\code{\link[=features]{features()}}

\code{\link[=features]{features()}}
}
