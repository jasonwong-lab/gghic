% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/setters-methods.R
\name{features<-}
\alias{features<-}
\alias{features-set}
\alias{features<-,ChromatinContacts,character,GRanges-method}
\alias{features<-,ChromatinContacts,character,GInteractions-method}
\alias{features<-,ChromatinContacts,character,GRangesList-method}
\alias{features<-,ChromatinContacts,character,list-method}
\title{Set genomic features in ChromatinContacts object}
\usage{
features(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,GRanges}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,GInteractions}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,GRangesList}(x, name, ...) <- value

\S4method{features}{ChromatinContacts,character,list}(x, name, ...) <- value
}
\arguments{
\item{x}{ChromatinContacts object with imported interaction data.}

\item{name}{Character. Feature slot name:
\itemize{
\item \code{"compartments"}: A/B compartment annotations (GRanges)
\item \code{"TADs"}: Topologically associating domains (GRanges)
\item \code{"multi_contacts"}: Multi-way contact regions (GRanges)
\item \code{"loops"}: Chromatin loop calls (GInteractions)
\item \code{"tracks"}: Genomic signal tracks (GRangesList)
}}

\item{...}{Additional arguments (not currently used).}

\item{value}{Feature data with appropriate type:
\itemize{
\item GRanges: for compartments, TADs, multi_contacts
\item GInteractions: for loops
\item GRangesList or list: for tracks (will be converted to GRangesList)
}}
}
\value{
ChromatinContacts object with updated features slot. The modified
object is returned invisibly to support piping.
}
\description{
Assigns or updates genomic features (TADs, loops, compartments, tracks) in
a ChromatinContacts object. Features are automatically subsetted to overlap
with loaded interactions, ensuring only relevant data is retained.
}
\details{
\subsection{Requirements}{

Interactions must be imported before assigning features. Use
\code{import()} first.
}

\subsection{Automatic subsetting}{

Features are automatically filtered to retain only elements overlapping the
loaded Hi-C interaction data. This prevents visualization errors and reduces
memory usage.
}

\subsection{Track naming}{

When providing tracks as GRangesList, ensure they are named for proper
labeling in plots. Unnamed tracks receive default names (\code{track1}, \code{track2},
etc.).
}
}
\examples{
\dontrun{
# Load Hi-C data first
cc <- ChromatinContacts("sample.cool") |> import()

# Add TADs from BED file
features(cc, "TADs") <- rtracklayer::import("TADs.bed")

# Add chromatin loops
features(cc, "loops") <- loops_ginteractions

# Add multiple ChIP-seq tracks
features(cc, "tracks") <- GRangesList(
  H3K27ac = rtracklayer::import("H3K27ac.bw"),
  H3K4me3 = rtracklayer::import("H3K4me3.bw")
)

# Add compartments
features(cc, "compartments") <- compartment_gr

# Chain operations
cc <- ChromatinContacts("sample.cool") |>
  import() |>
  `features<-`("TADs", value = tads) |>
  `features<-`("loops", value = loops)
}

}
\seealso{
\code{\link[=features]{features()}}, \code{\link[=ChromatinContacts]{ChromatinContacts()}}, \link{import-ChromatinContacts}
}
